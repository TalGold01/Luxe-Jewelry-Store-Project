name: CI/CD Pipeline - Luxe Jewelry Store

# 1. Trigger the workflow on pushes to the main branch OR a new tag
on:
  push:
    branches:
      - main
    tags:
      - 'v*' # Trigger on any tag starting with 'v' (e.g., v1.0.0)

# Define global environment variables
env:
  DOCKERHUB_REGISTRY: talgold01
  NEXUS_REGISTRY: localhost:8082/docker-hosted
  PROJECT_NAME: luxe-jewelry-store-project 
  DEPLOY_ENV: development
  
  # VERSION CONTROL LOGIC
  IMAGE_VERSION: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || github.run_id }} 

jobs:
  
  # ====================================================================
  # STABLE CI/CD PIPELINE JOB
  # ====================================================================
  ci_cd_pipeline:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4
      
      # 🔑 Set up Snyk Token once for all scans
      - name: 🔑 Set up Snyk
        uses: snyk/actions/setup@master
        with:
          snyk-token: ${{ secrets.SNYK_TOKEN }}

      # --- 🧪 UNIT TESTS STAGE ---

      - name: ⚙️ Set up Python Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      - name: 📦 Install Pytest Dependency
        run: pip install pytest 

      - name: 🧪 Run Unit Tests
        working-directory: test
        run: pytest --maxfail=1 --disable-warnings -q

      # --- 🏗️ BUILD STAGE (FIXED TAGGING SOURCE) ---

      - name: 🛠️ Setup Docker Compose
        run: docker compose version

      - name: 🏗️ Build and Tag Docker Images for Nexus
        env:
          AUTH_TAG: ${{ env.NEXUS_REGISTRY }}/auth-service:${{ env.IMAGE_VERSION }}
          BACKEND_TAG: ${{ env.NEXUS_REGISTRY }}/backend:${{ env.IMAGE_VERSION }}
          FRONTEND_TAG: ${{ env.NEXUS_REGISTRY }}/frontend:${{ env.IMAGE_VERSION }}
          
          # Define the exact source tags based on the build output for retagging
          # The images are named using the DOCKERHUB_REGISTRY/PROJECT_NAME-service-name:latest format
          AUTH_SOURCE_TAG: docker.io/${{ env.DOCKERHUB_REGISTRY }}/${{ env.PROJECT_NAME }}-auth-service:latest
          BACKEND_SOURCE_TAG: docker.io/${{ env.DOCKERHUB_REGISTRY }}/${{ env.PROJECT_NAME }}-backend:latest
          FRONTEND_SOURCE_TAG: docker.io/${{ env.DOCKERHUB_REGISTRY }}/${{ env.PROJECT_NAME }}-frontend:latest
          
        run: |
          cd infra
          echo "Starting Docker builds with tag: ${{ env.IMAGE_VERSION }}"
          
          # 1. Build images. These create the source images defined above.
          docker compose build \
            --build-arg IMAGE_VERSION=${{ env.IMAGE_VERSION }} \
            auth-service backend frontend 
          
          # 2. Manually tag the local images with the correct Nexus tag.
          # FIX: Use the exact image name reported by Docker Compose build output as the source.
          docker tag $AUTH_SOURCE_TAG $AUTH_TAG
          docker tag $BACKEND_SOURCE_TAG $BACKEND_TAG
          docker tag $FRONTEND_SOURCE_TAG $FRONTEND_TAG
          
          echo "All images successfully built and tagged for Nexus."

      # --- 🐳 SECURITY SCAN STAGE (Post-Build/Image-Based) ---

      - name: 🛡️ Snyk Final Image Scan (auth-service)
        uses: snyk/actions/docker@master
        env:
          AUTH_TAG: ${{ env.NEXUS_REGISTRY }}/auth-service:${{ env.IMAGE_VERSION }}
        with:
          image: ${{ env.AUTH_TAG }}
          severity: critical

      # --- 🚀 PUSH STAGE ---

      - name: 🔑 Log in to Nexus Repository (via Docker CLI)
        run: |
          echo "${{ secrets.NEXUS_PASSWORD }}" | docker login ${{ env.NEXUS_REGISTRY }} \
          --username ${{ secrets.NEXUS_USERNAME }} \
          --password-stdin
      
      - name: 🚀 Push Images to Nexus (Version Control Implemented)
        env:
          AUTH_TAG: ${{ env.NEXUS_REGISTRY }}/auth-service:${{ env.IMAGE_VERSION }}
          BACKEND_TAG: ${{ env.NEXUS_REGISTRY }}/backend:${{ env.IMAGE_VERSION }}
          FRONTEND_TAG: ${{ env.NEXUS_REGISTRY }}/frontend:${{ env.IMAGE_VERSION }}
        run: |
          docker push $AUTH_TAG
          docker push $BACKEND_TAG
          docker push $FRONTEND_TAG

      # --- ☁️ DEPLOYMENT & CLEANUP ---
      
      - name: ☁️ Deploy Services (Placeholder for Remote Deploy)
        run: |
          echo "Deployment to ${{ env.DEPLOY_ENV }} initiated using images tagged with version: ${{ env.IMAGE_VERSION }}"

      - name: 🧹 Cleanup Docker Resources
        if: always()
        run: docker system prune -af